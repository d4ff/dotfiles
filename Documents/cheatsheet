# tmux
C-b %   Split
C-b "   Vertical split
C-b c   New window
C-b p | n   Switch window
C-b 12345   select window
C-b d | D   Detach current sesh | detach x

^K        Make current window topleft+maxwidth
:res X
:vertical resize X
^o        Close all but current
:so % - Update init.vim (when init.vim open)


# CtrlP and buffergator buffer management
leader b    Show all buffers
  ^v        Open buffer as vsplit
  ^s        Open buffer as split
  ^t        Opten buffer as fullscrn
leader o    search drive for files
leader O    search buffers for file
gt / gT     switch tab

# Nvim-r
\aa       Send file
\l        Send line
\ss       Send selection

\rr       Clear console
\ro       Oject browser
\rs       Summary
\rn       Names
\rp       Print
\rl       ?list
mtrace(function_name)   # Debug mode of a function ('debug' plugin)
count(ds, var) gir freq tabel (plyr package)


# Markdown
:Mpreview   to launch preview in browser

# Pandoc
pandoc --filter pandoc-citeproc input.md -s -o output.html#
